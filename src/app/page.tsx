/**
 * TaskKu — Root Page
 *
 * Halaman ini tidak akan pernah dirender karena middleware
 * menangkap "/" dan langsung redirect ke /dashboard atau /login.
 */

export default function HomePage() {
  return null;
}
