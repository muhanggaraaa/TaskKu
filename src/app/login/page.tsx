/**
 * Login Page — Server Component
 * 
 * Halaman login publik yang menampilkan form login.
 * Menggunakan LoginForm (Client Component) untuk interaktivitas.
 */

import { LoginForm } from "./LoginForm";

export default function LoginPage() {
  return <LoginForm />;
}
